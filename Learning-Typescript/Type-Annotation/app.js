//? Example no 1
var collectTenCharacter = function (str) {
    return str.substring(0, 12);
};
//
var longText = "This is a long string and we want to collect only ten characters";
var result = collectTenCharacter(longText);
console.log(result);
//? Example no 2 
var longstr = "This is a long string and we want to collect only ten char";
var shortstr = "";
//todo use for loop solve this question
